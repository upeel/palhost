export type Product = {
  id: number;
  name: string;
  code: string;
  price: number;
  description: string;
  period: number;
  region: string;
  is_active: number;
  is_available: Boolean;
  exchange_prices: any;
};
